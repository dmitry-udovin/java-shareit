server:
  port: 8080

shareit:
  server:
    url: ${SHAREIT_SERVER_URL:http://localhost:9090}

spring:
  main:
    banner-mode: off
  jmx:
    enabled: false

logging:
  level:
    org.springframework: WARN
    org.apache.catalina: WARN
